# Fonts

Drop the font files you want to use for PDF generation into this folder.

Expected files (example):
- Regular.ttf
- Bold.ttf
- Italic.ttf
- BoldItalic.ttf

Once added, tell me the exact filenames and I will wire them up in
`src/lib/prescriptions/generatePrescription.ts`.
